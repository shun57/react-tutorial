import React from 'react';
import Game from '../Game';

export default {
  component: Game,
  title: 'SquareGame/Game',
};

const Template = () => <Game />;

export const Default = Template.bind({});
